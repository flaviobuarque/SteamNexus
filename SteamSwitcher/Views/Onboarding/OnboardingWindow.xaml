<ui:FluentWindow
    x:Class="SteamSwitcher.Views.Onboarding.OnboardingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
    xmlns:vm="clr-namespace:SteamSwitcher.ViewModels"
    xmlns:steps="clr-namespace:SteamSwitcher.Views.Onboarding.Steps"
    xmlns:converters="clr-namespace:SteamSwitcher.Converters"
    Title="Steam Switcher — Configuração inicial"
    Width="680"
    Height="640"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    ExtendsContentIntoTitleBar="True">

    <ui:FluentWindow.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility" />
    </ui:FluentWindow.Resources>

    <Grid Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- TitleBar -->
        <ui:TitleBar Grid.Row="0" Title="Configuração inicial" />

        <!-- Steps indicator -->
        <Border
            Grid.Row="1"
            Padding="40,20,40,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!-- Step dots + labels -->
                <ItemsControl
                    Grid.Column="0"
                    Grid.ColumnSpan="5"
                    ItemsSource="{Binding StepItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="1" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel HorizontalAlignment="Center">
                                <!-- Bolinha -->
                                <Border
                                    Width="28" Height="28"
                                    CornerRadius="14"
                                    HorizontalAlignment="Center"
                                    BorderThickness="2">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background"
                                                Value="{ui:ThemeResource ControlFillColorDefaultBrush}" />
                                            <Setter Property="BorderBrush"
                                                Value="{ui:ThemeResource ControlStrokeColorDefaultBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCompleted}" Value="True">
                                                    <Setter Property="Background"
                                                        Value="{ui:ThemeResource SystemAccentColorPrimaryBrush}" />
                                                    <Setter Property="BorderBrush"
                                                        Value="{ui:ThemeResource SystemAccentColorPrimaryBrush}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                    <Setter Property="Background"
                                                        Value="{ui:ThemeResource SystemAccentColorPrimaryBrush}" />
                                                    <Setter Property="BorderBrush"
                                                        Value="{ui:ThemeResource SystemAccentColorPrimaryBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <!-- Número ou checkmark -->
                                    <Grid>
                                        <TextBlock
                                            Text="{Binding Number}"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            FontSize="13"
                                            FontWeight="SemiBold"
                                            Visibility="{Binding IsCompleted,
                                                Converter={StaticResource InverseBoolToVisibility}}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Foreground" Value="{DynamicResource AppTextPrimaryBrush}" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                            <Setter Property="Foreground" Value="White" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <ic:SymbolIcon
                                            Symbol="Checkmark"
                                            FontSize="14"
                                            Foreground="White"
                                            HorizontalAlignment="Center"
                                            VerticalAlignment="Center"
                                            Visibility="{Binding IsCompleted,
                                                Converter={StaticResource BoolToVisibility}}" />
                                    </Grid>
                                </Border>

                                <!-- Label -->
                                <TextBlock
                                    Text="{Binding Label}"
                                    FontSize="11"
                                    HorizontalAlignment="Center"
                                    Margin="0,6,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Opacity" Value="0.4" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                                    <Setter Property="Opacity" Value="1" />
                                                    <Setter Property="FontWeight" Value="SemiBold" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Linha conectora entre bolinhas -->
                <Rectangle
                    Grid.Column="0"
                    Grid.ColumnSpan="5"
                    Height="2"
                    VerticalAlignment="Top"
                    Margin="40,13,40,0"
                    Fill="{ui:ThemeResource ControlStrokeColorDefaultBrush}"
                    Panel.ZIndex="-1" />
            </Grid>
        </Border>

        <!-- Conteúdo do step atual -->
        <ContentControl
            Grid.Row="2"
            Margin="0,20,0,0"
            Content="{Binding CurrentStepView}" />

        <!-- Navegação -->
        <Border
            Grid.Row="3"
            Padding="40,16"
            BorderThickness="0,1,0,0"
            BorderBrush="{ui:ThemeResource ControlStrokeColorDefaultBrush}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:Button
                    Cursor="Hand"
                    Grid.Column="0"
                    Content="Voltar"
                    Appearance="Secondary"
                    Command="{Binding PreviousStepCommand}"
                    Visibility="{Binding CanGoBack, Converter={StaticResource BoolToVisibility}}" />

                <ui:Button
    Grid.Column="2"
    Padding="24,8"
    Command="{Binding NextStepCommand}"
    IsEnabled="{Binding CanGoNext}"
    Visibility="{Binding IsLastStep, Converter={StaticResource InverseBoolToVisibility}}"
    Cursor="Hand">
                    <ui:Button.Style>
                        <Style TargetType="ui:Button" BasedOn="{StaticResource {x:Type ui:Button}}">
                            <Setter Property="Background" Value="#3e8fe1" />
                            <Setter Property="MouseOverBackground" Value="#5aa3e8" />
                            <Setter Property="PressedBackground" Value="#2d7acc" />
                            <Setter Property="Foreground" Value="White" />
                            <Setter Property="BorderBrush" Value="#3e8fe1" />
                            <Style.Triggers>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="{DynamicResource ControlFillColorDefaultBrush}" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource TextFillColorTertiaryBrush}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ui:Button.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Continuar" VerticalAlignment="Center" />
                        <ic:SymbolIcon
            Symbol="ArrowRight"
            FontSize="14"
            Margin="8,0,0,0"
            VerticalAlignment="Center" />
                    </StackPanel>
                </ui:Button>

                <ui:Button
                    Cursor="Hand"
                    Grid.Column="2"
                    Content="Concluir"
                    Style="{StaticResource SteamSwitcherPrimaryButtonStyle}"
                    Padding="24,8"
                    Command="{Binding CompleteCommand}"
                    Visibility="{Binding IsLastStep, Converter={StaticResource BoolToVisibility}}" />
            </Grid>
        </Border>
    </Grid>
</ui:FluentWindow>
