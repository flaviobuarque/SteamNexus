<Window
    x:Class="SteamSwitcher.Views.Dialogs.PickAccountDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:ic="clr-namespace:FluentIcons.Wpf;assembly=FluentIcons.Wpf"
    WindowStyle="None"
    AllowsTransparency="True"
    Background="Transparent"
    ResizeMode="NoResize"
    Width="800"
    Height="600"
    WindowStartupLocation="CenterOwner"
    ShowInTaskbar="False">

	<Window.Effect>
		<DropShadowEffect
            Color="Transparent"
            BlurRadius="0"
            ShadowDepth="0" />
	</Window.Effect>

	<Border Background="{DynamicResource AppDialogBackdropBrush}">
		<Border
            Background="{DynamicResource AppChromeBrush}"
            BorderBrush="{DynamicResource AppBorderBrush}"
            BorderThickness="1"
            CornerRadius="14"
            Padding="24"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Margin="0"
            MinWidth="360"
			x:Name="DialogCard"
            Opacity="0">

			<Border.RenderTransform>
				<ScaleTransform ScaleX="1" ScaleY="1" />
			</Border.RenderTransform>

			<StackPanel>

				<!-- Header -->
				<StackPanel Orientation="Horizontal" Margin="0,0,0,6">
					<ui:SymbolIcon
                        Symbol="PersonQuestionMark24"
                        Foreground="{DynamicResource AppAccentAltBrush}"
                        Margin="0,0,10,0"
                        VerticalAlignment="Center" />
					<TextBlock
                        Text="Associar conta"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource AppTextPrimaryBrush}"
                        VerticalAlignment="Center" />
				</StackPanel>

				<TextBlock
                    FontSize="13"
                    Foreground="{DynamicResource AppTextSecondaryBrush}"
                    TextWrapping="Wrap"
                    Margin="0,0,0,4">
					<Run Text="Qual conta deve jogar" />
					<Run Text="{Binding GameName, Mode=OneWay}"
                        FontWeight="SemiBold"
                        Foreground="{DynamicResource AppTextPrimaryBrush}" />
					<Run Text="?" />
				</TextBlock>

				<TextBlock
                    Text="Este jogo não possui conta associada no appmanifest."
                    FontSize="11"
                    Foreground="{DynamicResource AppTextMutedBrush}"
                    Margin="0,2,0,16"
                    TextWrapping="Wrap" />

                <ui:TextBox
                    Margin="0,0,0,12"
                    PlaceholderText="Buscar conta..."
                    Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}">
                    <ui:TextBox.Icon>
                        <ui:SymbolIcon Symbol="Search24" />
                    </ui:TextBox.Icon>
                </ui:TextBox>

                <ScrollViewer
                    MaxHeight="280"
                    VerticalScrollBarVisibility="Auto"
                    HorizontalScrollBarVisibility="Disabled"
                    Margin="0,0,0,8">
                    <!-- Lista de contas -->
                    <ItemsControl ItemsSource="{Binding Accounts}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border
                                    Margin="0,0,12,8"
                                    Padding="14,11"
                                    CornerRadius="10"
                                    BorderThickness="2"
                                    Cursor="Hand">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="Background" Value="{DynamicResource AppSurfaceBrush}" />
                                            <Setter Property="BorderBrush" Value="{DynamicResource AppBorderBrush}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="BorderBrush" Value="{DynamicResource AppAccentBrush}" />
                                                    <Setter Property="Background" Value="{DynamicResource AppAccentSurfaceBrush}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Border.InputBindings>
                                        <MouseBinding
                                            MouseAction="LeftClick"
                                            Command="{Binding DataContext.SelectAccountCommand,
                                                RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}" />
                                    </Border.InputBindings>
                                    <StackPanel Orientation="Horizontal">
                                        <Grid Width="38" Height="38" Margin="0,0,12,0">
                                            <Ellipse Fill="{DynamicResource AppSurfaceAltBrush}" />
                                            <Image Source="{Binding Card.AvatarImage}" Stretch="UniformToFill">
                                                <Image.Clip>
                                                    <EllipseGeometry Center="19,19" RadiusX="19" RadiusY="19" />
                                                </Image.Clip>
                                            </Image>
                                            <ui:SymbolIcon
                                                Symbol="Person24"
                                                Foreground="{DynamicResource AppTextSecondaryBrush}"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Visibility="{Binding Card.HasAvatar, Converter={StaticResource InverseBoolToVisibility}}" />
                                        </Grid>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock
                                                Text="{Binding DisplayName}"
                                                FontWeight="SemiBold"
                                                FontSize="13"
                                                Foreground="{DynamicResource AppTextPrimaryBrush}" />
                                            <TextBlock
                                                Text="{Binding AccountName, StringFormat=@{0}}"
                                                FontSize="11"
                                                Foreground="{DynamicResource AppTextMutedBrush}"
                                                Margin="0,2,0,0" />
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl> 
                </ScrollViewer>

                <Grid Margin="0,16,0,0">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="10" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<ui:Button
                        Grid.Column="0"
                        Content="Cancelar"
                        Appearance="Secondary"
                        HorizontalAlignment="Stretch"
                        Padding="0,10"
                        Click="CancelButton_Click" />
					<ui:Button
                        Grid.Column="2"
                        Content="Salvar"
                        Style="{StaticResource SteamSwitcherPrimaryButtonStyle}"
                        HorizontalAlignment="Stretch"
                        Padding="0,10"
                        IsEnabled="{Binding HasSelection}"
                        Click="ConfirmButton_Click" />
				</Grid>

			</StackPanel>
		</Border>
	</Border>
</Window>
